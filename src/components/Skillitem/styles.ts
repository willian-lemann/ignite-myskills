import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listTile: {
    backgroundColor: "#1f1e25",
    padding: 15,
    borderRadius: 50,
    alignItems: "center",
    marginVertical: 10,
  },

  skillTitle: {
    color: "#fff",
    fontSize: 22,
    fontWeight: "bold",
  },
});
